
  # Invoice Tracking Platform

  This is a code bundle for Invoice Tracking Platform. The original project is available at https://www.figma.com/design/pkOa7i1UOAlIeuRVNx2CkR/Invoice-Tracking-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  